local Base = require "core.Object"
local SystemData = class(Base)

function SystemData:ctor(...)
    Base.ctor(self, ...)
end

function SystemData:ClearData()
end

function SystemData:OnDispose()
    Base:OnDispose()
end

return SystemData
