local require = require
local Base = require "GameData.SystemData"
local GameState = class(Base)

function GameState:ctor(ui, mgr)
    Base.ctor(self, ui)
    self.mgr = mgr
end


function GameState:ChangeState(stateName)
    (self.mgr):ChangeState(stateName)
end

return GameState
