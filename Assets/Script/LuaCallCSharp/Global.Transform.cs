using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;
using UnityEngine.EventSystems;
using XLua;

// 设置时间的时间类 
public static partial class Global
{

    public static void SetLocalPos(string ui, int linkid, int tid, int x, int y, int z)
    {
        //if (!UIMgr)
        //    return;
        //CGameUI UI = UIMgr.Get(ui);
        //if (UI)
        //    (UI as CGameLuaUI).SetLocalPos(linkid, tid, x, y, z);
    }

}
