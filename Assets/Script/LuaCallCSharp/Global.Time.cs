using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;
using UnityEngine.EventSystems;
using XLua;

// 设置时间的时间类 
public static partial class  Global
{

    //public static void SetTimeHourString(string ui, int linkid, int tid, int seconds)
    //{
    //    if (!UIMgr)
    //        return;
    //    CGameUI UI = UIMgr.Get(ui);
    //    if (UI)
    //        (UI as CGameLuaUI).SetText(linkid, tid, TimeHelper.GetTimeHourString(seconds));
    //}

    //public static void SetTimeString(string ui, int linkid, int tid, int seconds)
    //{
    //    if (!UIMgr)
    //        return;
    //    CGameUI UI = UIMgr.Get(ui);
    //    if (UI)
    //        (UI as CGameLuaUI).SetText(linkid, tid, TimeHelper.GetTimeString(seconds));
    //}




}
